vector<int> separateNegativeAndPositive(vector<int> &nums){
    // Write your code here.
    int n = nums.size();
    int pivot = 0;
    for(int i = 0;i<n;i++){
        if(nums[i] < 0){
            swap(nums[i],nums[pivot++]);
            }
            }    
    return nums; 
}
